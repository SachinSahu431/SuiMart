{
  "name": "pinata-server",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.7.9",
    "pinata": "^2.2.2"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
